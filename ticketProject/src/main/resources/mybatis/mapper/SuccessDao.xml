<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.ticket.t1.dao.ISuccessDao">

	<resultMap id="successMap1" type="java.util.HashMap"></resultMap>
	
	<select id="getSuccessListBySusseq" statementType="CALLABLE" parameterType="java.util.HashMap">
		{
			CALL getSuccessListBySusseq(
				#{susseq},
				#{ref_cursor, mode=OUT, jdbcType=CURSOR, javaType=ResultSet, resultMap=successMap1}
			)
		}
	</select>

</mapper>